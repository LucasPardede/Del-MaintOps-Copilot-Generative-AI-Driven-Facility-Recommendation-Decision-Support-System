[project]
name = "del-maintops-copilot"
version = "0.1.0"
description = "Del-MaintOps Copilot - Milestone 1: Problem Framing, PEAS, dan Baseline Search (UCS)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/search"]
